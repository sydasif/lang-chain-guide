[project]
name = "lang-chain"
version = "0.1.0"
description = "LangChain Complete Guide"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.1.0",
    "langchain-groq>=1.1.0",
    "python-dotenv>=1.2.1",
]
